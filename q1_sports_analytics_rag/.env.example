# OpenAI Configuration
# Required: Your OpenAI API key for accessing GPT models
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI model to use (default: gpt-4-turbo-preview)
OPENAI_MODEL=gpt-4-turbo-preview

# Vector Database Configuration
# Optional: Directory to persist ChromaDB data (default: ./chroma_db)
CHROMA_PERSIST_DIRECTORY=./chroma_db

# Optional: Collection name for the vector database (default: sports_analytics)
COLLECTION_NAME=sports_analytics

# Embedding Configuration
# Optional: Embedding model to use (default: sentence-transformers/all-MiniLM-L6-v2)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# RAG Configuration
# Optional: Size of text chunks for processing (default: 1000)
CHUNK_SIZE=1000

# Optional: Overlap between chunks (default: 200)
CHUNK_OVERLAP=200

# Optional: Number of documents to retrieve (default: 10)
TOP_K_RETRIEVAL=10

# Optional: Number of documents to rerank (default: 5)
TOP_K_RERANK=5

# Query Decomposition
# Optional: Maximum number of sub-questions to generate (default: 5)
MAX_SUB_QUESTIONS=5

# Context Compression
# Optional: Compression ratio for context (default: 0.7)
COMPRESSION_RATIO=0.7

# API Configuration
# Optional: Host for the FastAPI server (default: 0.0.0.0)
API_HOST=0.0.0.0

# Optional: Port for the FastAPI server (default: 8000)
API_PORT=8000